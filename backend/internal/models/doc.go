// Package models contains shared domain types used across the handler,
// service, and repository layers. Models must not import any other internal
// package to avoid circular dependencies.
package models
